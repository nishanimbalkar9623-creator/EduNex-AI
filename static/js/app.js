window.EduNex = { async requestJson(url, options = {}) { const response = await fetch(url, { headers: { "Content-Type": "application/json" }, ...options }); const data = await response.json(); if (!response.ok) throw new Error(data.error || "Something went wrong."); return data; } };
const loginForm = document.querySelector("#login-form");
if (loginForm) loginForm.addEventListener("submit", async (event) => { event.preventDefault(); const error = document.querySelector("#login-error"); const submit = loginForm.querySelector("button[type='submit']"); error.textContent = ""; submit.disabled = true; try { await window.EduNex.requestJson("/api/auth/login", { method: "POST", body: JSON.stringify({ username: document.querySelector("#username").value, password: document.querySelector("#password").value }) }); window.location.assign("/dashboard"); } catch (requestError) { error.textContent = requestError.message; submit.disabled = false; } });
const logoutButton = document.querySelector("#logout-button");
if (logoutButton) logoutButton.addEventListener("click", async () => { await window.EduNex.requestJson("/api/auth/logout", { method: "POST" }); window.location.assign("/"); });
